<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".view.MainActivity">

    <TextView
        android:id="@+id/tv_select_module"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@android:color/holo_orange_light"
        android:text="@string/select_module"
        android:textSize="@dimen/text_size"
        android:layout_marginTop="@dimen/elem_margin_top"
        android:layout_centerHorizontal="true"/>

    <Spinner
        android:id="@+id/spn_modules"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/tv_select_module"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="@dimen/elem_margin_top"/>

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/btn_add_module"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:fabSize="normal"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_marginRight="@dimen/elem_margin_top"
        android:layout_marginBottom="@dimen/elem_margin_top"
        android:src="@drawable/ic_add"/>

    <TextView
        android:id="@+id/textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignEnd="@+id/spn_modules"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="41dp"
        android:text="@string/add_new_module" />

</RelativeLayout>